#include "greedy.hpp"

namespace greedy {
  std::vector<std::pair<Courier, Order>> greedy_approach (const std::vector<Courier>& couriers, std::vector<Order>& orders) {
    std::vector<std::pair<Courier, Order>> greedy_assignment;

    for(auto& order: orders) {
    }
  }
}
